name: Lexia Premium Drawing Actions Browser

on:
  pull_request:
    paths:
      - 'src/components/game/DrawingCanvas.jsx'
      - 'src/components/game/GameActionButton.jsx'
      - 'src/pages/PlayGame.jsx'
      - 'src/styles/premium-game.css'
      - 'scripts/check-premium-drawing-actions-browser.mjs'
      - '.github/workflows/premium-drawing-actions-browser.yml'
  workflow_dispatch:

jobs:
  premium-drawing-actions-browser:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - name: Install dependencies
        run: npm ci
      - name: Dependency security gate
        run: npm audit --audit-level=high
      - name: Typecheck UI
        run: npm run typecheck
      - name: Typecheck core
        run: npm run typecheck:core
      - name: Premium UI foundation contract
        run: node scripts/check-premium-ui-foundation.mjs
      - name: No-gradient learner chrome audit
        run: node scripts/check-no-gradient-learner-chrome.mjs
      - name: Prepare Chrome runtime
        run: |
          if command -v google-chrome >/dev/null 2>&1; then
            echo "CHROME_BIN=$(command -v google-chrome)" >> "$GITHUB_ENV"
          elif command -v chromium >/dev/null 2>&1; then
            echo "CHROME_BIN=$(command -v chromium)" >> "$GITHUB_ENV"
          else
            sudo apt-get update
            sudo apt-get install -y chromium-browser || sudo apt-get install -y chromium
            echo "CHROME_BIN=$(command -v chromium-browser || command -v chromium)" >> "$GITHUB_ENV"
          fi
      - name: Premium drawing actions browser QA
        run: node scripts/check-premium-drawing-actions-browser.mjs
      - name: Upload M38-E evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: m38e-premium-drawing-actions
          path: artifacts/m38e-drawing
          if-no-files-found: warn
